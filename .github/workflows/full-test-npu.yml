name: Full Test (NPU)

on:
#  pull_request:
#    branches:
#      - main
#      - testcases
#      - testcases-260813
#    paths:
#      - ".github/workflows/full-test-npu.yml"
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git ref (branch, tag, or SHA) to test. If not provided, uses the default branch.'
        required: false
        type: string
        default: ''
      job_filter:
        description: 'Select which job to run (leave empty or "all" to run all jobs)'
        required: false
        type: string
        default: 'all'
      image_a2:
        description: 'The a2 running docker image of the test task.'
        required: false
        type: string
        default: 'swr.cn-southwest-2.myhuaweicloud.com/base_image/dockerhub/lmsysorg/sglang:main-cann9.0.0-910b'
      image_a3:
        description: 'The a3 running docker image of the test task.'
        required: false
        type: string
        default: 'swr.cn-southwest-2.myhuaweicloud.com/base_image/dockerhub/lmsysorg/sglang:main-cann9.0.0-a3'
      skip_install_flag:
        description: 'Indicates whether to skip the installation of sglang, defaulting to false.'
        required: false
        type: string
        default: ''
      test_scope:
        description: 'Test scope: all / func / poc / no_nightly / poc_no_nightly'
        required: true
        type: choice
        options:
          - all
          - func
          - poc
          - no_nightly
          - poc_no_nightly
        default: 'poc'

concurrency:
  group: full-test-npu-${{ inputs.ref || github.ref }}
  cancel-in-progress: ${{ github.event_name != 'workflow_call' }}

jobs:
  set-image-config:
    runs-on: ubuntu-latest
    outputs:
      ref: ${{ steps.set-vars.outputs.ref }}
      job_filter: ${{ steps.set-vars.outputs.job_filter }}
      image_a2: ${{ steps.set-vars.outputs.image_a2 }}
      image_a3: ${{ steps.set-vars.outputs.image_a3 }}
      skip_install_flag: ${{ steps.set-vars.outputs.skip_install_flag }}
      test_scope: ${{ steps.set-vars.outputs.test_scope }}
      run_start_metadata: ${{ steps.set-vars.outputs.run_start_metadata }}
    steps:
      # When triggered by PR, no inputs parameters are used. The latest community code is tested by default.
      - name: Set image config
        id: set-vars
        env:
          # Pass untrusted PR metadata through environment variables to avoid shell‑script interpolation,
          # mitigating script injection risks from fork branch names.
          HEAD_LABEL: ${{ github.event.pull_request.head.label }}
        run: |
          echo "========== INPUT PARAMETERS =========="
          echo "inputs.ref            = ${{ inputs.ref }}"
          echo "inputs.job_filter     = ${{ inputs.job_filter }}"
          echo "inputs.image_a2        = ${{ inputs.image_a2 }}"
          echo "inputs.image_a3        = ${{ inputs.image_a3 }}"
          echo "inputs.skip_install_flag = ${{ inputs.skip_install_flag }}"
          echo "inputs.test_scope      = ${{ inputs.test_scope }}"
          echo "======================================="
          if [ -z "${{ inputs.ref }}" ]; then
            echo "ref=" >> $GITHUB_OUTPUT
          else
            echo "ref=${{ inputs.ref }}" >> $GITHUB_OUTPUT
          fi

          if [ -z "${{ inputs.job_filter }}" ]; then
            echo "job_filter=all" >> $GITHUB_OUTPUT
          else
            echo "job_filter=${{ inputs.job_filter }}" >> $GITHUB_OUTPUT
          fi

          if [ -z "${{ inputs.image_a2 }}" ]; then
            echo "image_a2=swr.cn-southwest-2.myhuaweicloud.com/base_image/dockerhub/lmsysorg/sglang:main-cann9.0.0-910b" >> $GITHUB_OUTPUT
          else
            echo "image_a2=${{ inputs.image_a2 }}" >> $GITHUB_OUTPUT
          fi

          if [ -z "${{ inputs.image_a3 }}" ]; then
            echo "image_a3=swr.cn-southwest-2.myhuaweicloud.com/base_image/dockerhub/lmsysorg/sglang:main-cann9.0.0-a3" >> $GITHUB_OUTPUT
          else
            echo "image_a3=${{ inputs.image_a3 }}" >> $GITHUB_OUTPUT
          fi

          if [ -z "${{ inputs.skip_install_flag }}" ]; then
            echo "skip_install_flag=false" >> $GITHUB_OUTPUT
          else
            echo "skip_install_flag=${{ inputs.skip_install_flag }}" >> $GITHUB_OUTPUT
          fi

          if [ -z "${{ inputs.test_scope }}" ]; then
            echo "test_scope=poc" >> $GITHUB_OUTPUT
          else
            echo "test_scope=${{ inputs.test_scope }}" >> $GITHUB_OUTPUT
          fi

          # If the nightly pipeline is triggered by a PR, this variable takes the source branch label of the PR;
          # otherwise, it uses the branch or tag name of the current ref.
          BRANCH_LABEL="$HEAD_LABEL"
          if [ -z "${BRANCH_LABEL}" ]; then
            BRANCH_LABEL="${{ github.ref_name }}"
          fi
          BRANCH_LABEL="$(echo "${BRANCH_LABEL}" | tr -cd 'a-zA-Z0-9._/:' | tr '/:' '--')"
          # Package run metadata (branch/workflow/create_time) as a single JSON for reuse by all
          # test jobs; create_time is UTC+8 date + time (minute precision) recorded once at run
          # start, shared even across midnight.
          RUN_START_METADATA="$(python3 -c "import json,sys; print(json.dumps({'branch_label':sys.argv[1],'workflow_name':sys.argv[2],'create_time':sys.argv[3]}))" "$BRANCH_LABEL" "${{ github.workflow }}" "$(date -u -d '+8 hours' +%Y%m%d-%H%M)")"
          # Write to GITHUB_OUTPUT and print to the log in one command.
          echo "run_start_metadata=${RUN_START_METADATA}" | tee -a $GITHUB_OUTPUT

  nighly-test-npu:
    needs: [ set-image-config ]
    name: nightly-test-npu
    if: ${{ needs.set-image-config.outputs.test_scope == 'all' || needs.set-image-config.outputs.test_scope == 'poc' }}
    uses: ./.github/workflows/nightly-test-npu.yml
    with:
      ref: ${{ needs.set-image-config.outputs.ref }}
      job_filter: ${{ needs.set-image-config.outputs.job_filter }}
      image_a2: ${{ needs.set-image-config.outputs.image_a2 }}
      image_a3: ${{ needs.set-image-config.outputs.image_a3 }}
      skip_install_flag: ${{ needs.set-image-config.outputs.skip_install_flag }}
    secrets: inherit

  full-1-npu-a3:
    needs: [ set-image-config ]
    runs-on: linux-aarch64-a3-2-
    strategy:
      fail-fast: false
      matrix:
        part: [0, 1]
    if: ${{ needs.set-image-config.outputs.test_scope == 'all' || needs.set-image-config.outputs.test_scope == 'func' || needs.set-image-config.outputs.test_scope == 'no_nightly' }}
    container:
      image: ${{ needs.set-image-config.outputs.image_a3 }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.set-image-config.outputs.ref || github.ref }}
      - name: Install dependencies
        env:
          TORCH_CACHE_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/whl/cpu"
          PYPI_CACHE_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple"
          UV_INDEX_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple"
          GITHUB_PROXY_URL: "https://gh-proxy.test.osinfra.cn/"
        run: |
          # speed up by using infra cache services
          CACHING_URL="cache-service.nginx-pypi-cache.svc.cluster.local"
          sed -Ei "s@(ports|archive).ubuntu.com@${CACHING_URL}:8081@g" /etc/apt/sources.list
          pip config set global.index-url http://${CACHING_URL}/pypi/simple
          pip config set global.trusted-host "${CACHING_URL}"
          sglang_source_path=$(pwd)
          echo "Source code path: ${sglang_source_path}"
          if [ ${{ needs.set-image-config.outputs.skip_install_flag }} != "true" ];then
            bash scripts/ci/npu/npu_ci_install_dependency.sh a3
          else
            sglang_pkg_path=/sgl-workspace/sglang/python
            ascend_test_util_path=${sglang_pkg_path}/sglang/test/ascend
            mkdir -p ${ascend_test_util_path}
            mv ${ascend_test_util_path} ${ascend_test_util_path}_bak
            cp -r ${sglang_source_path}/python/sglang/test/ascend ${ascend_test_util_path}
          fi

          # copy required file from our daily cache
          cp ~/.cache/modelscope/hub/datasets/otavia/ShareGPT_Vicuna_unfiltered/ShareGPT_V3_unfiltered_cleaned_split.json /tmp
          cp ~/.cache/modelscope/hub/datasets/tmp/test.jsonl /tmp
          cp ~/.cache/modelscope/hub/datasets/hellaswag_val.jsonl /tmp
      - name: Print Log Information
        run: bash scripts/ci/npu/npu_log_print.sh
      - name: Run test
        timeout-minutes: 960
        env:
          SGLANG_USE_MODELSCOPE: true
          SGLANG_IS_IN_CI: true
          HF_ENDPOINT: https://hf-mirror.com
          TORCH_EXTENSIONS_DIR: /tmp/torch_extensions
          PYTORCH_NPU_ALLOC_CONF: "expandable_segments:True"
          STREAMS_PER_DEVICE: 32
          TRANSFORMERS_VERBOSITY: "error"
          GDN_ATTN_BACKEND_TRITON: 1
        run: |
          # speed up by using infra cache services
          pip install sglang_router
          hf download lmms-lab/MMMU --repo-type dataset
          pip install fastokens
          pip install mindstudio-probe --pre
          pip install sentence_transformers torchaudio==2.10.0
          pip install zss pre-commit wandb>=0.16.0 tenacity==8.3.0 loguru openpyxl latex2sympy2 zstandard transformers-stream-generator tqdm-multiprocess pycocoevalcap
          pip install yt-dlp sentencepiece==0.1.99 nltk av ftfy sqlitedict==2.1.0 sacrebleu>=1.5.0 pytablewriter black==24.1.0 isort==5.13.2 peft>=0.2.0 accelerate>=0.29.1
          pip install jsonlines httpx==0.25.0 evaluate>=0.4.0 datasets==2.16.1 numexpr xgrammar==0.2.1 numpy==1.26.4 dotenv granian
          pip install opentelemetry-sdk opentelemetry-api opentelemetry-exporter-otlp opentelemetry-exporter-otlp-proto-grpc
          pip install msgpack
          pip install watchfiles
          pip install torchao==0.16.0
          pip install /root/.cache/.cache/npu-pkgs/torch_memory_saver-0.0.8-cp312-cp312-linux_aarch64.whl
          git clone --branch v0.3.3 --depth 1 https://github.com/EvolvingLMMs-Lab/lmms-eval.git
          cd ./lmms-eval
          nohup pip install . > lmmslog.txt 2>&1 &
          sleep 120
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          cd ../
          cd test
          python3 run_suite.py --hw npu --suite full-1-npu-a3 --nightly --continue-on-error --timeout-per-file 4800 --auto-partition-size 2 --auto-partition-id ${{ matrix.part }}

  full-2-npu-a3:
    needs: [ set-image-config ]
    runs-on: linux-aarch64-a3-2-
    strategy:
      fail-fast: false
      matrix:
        part: [0, 1]
    if: ${{ needs.set-image-config.outputs.test_scope == 'all' || needs.set-image-config.outputs.test_scope == 'func' || needs.set-image-config.outputs.test_scope == 'no_nightly' }}
    container:
      image: ${{ needs.set-image-config.outputs.image_a3 }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.set-image-config.outputs.ref || github.ref }}
      - name: Install dependencies
        env:
          TORCH_CACHE_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/whl/cpu"
          PYPI_CACHE_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple"
          UV_INDEX_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple"
          GITHUB_PROXY_URL: "https://gh-proxy.test.osinfra.cn/"
        run: |
          CACHING_URL="cache-service.nginx-pypi-cache.svc.cluster.local"
          sed -Ei "s@(ports|archive).ubuntu.com@${CACHING_URL}:8081@g" /etc/apt/sources.list
          pip config set global.index-url http://${CACHING_URL}/pypi/simple
          pip config set global.trusted-host "${CACHING_URL}"
          sglang_source_path=$(pwd)
          if [ ${{ needs.set-image-config.outputs.skip_install_flag }} != "true" ];then
            bash scripts/ci/npu/npu_ci_install_dependency.sh a3
          else
            sglang_pkg_path=/sgl-workspace/sglang/python
            ascend_test_util_path=${sglang_pkg_path}/sglang/test/ascend
            mkdir -p ${ascend_test_util_path}
            mv ${ascend_test_util_path} ${ascend_test_util_path}_bak
            cp -r ${sglang_source_path}/python/sglang/test/ascend ${ascend_test_util_path}
          fi
          cp ~/.cache/modelscope/hub/datasets/otavia/ShareGPT_Vicuna_unfiltered/ShareGPT_V3_unfiltered_cleaned_split.json /tmp
          cp ~/.cache/modelscope/hub/datasets/tmp/test.jsonl /tmp
      - name: Print Log Information
        run: bash scripts/ci/npu/npu_log_print.sh
      - name: Run test
        timeout-minutes: 960
        env:
          SGLANG_USE_MODELSCOPE: true
          SGLANG_IS_IN_CI: true
          HF_ENDPOINT: https://hf-mirror.com
          TORCH_EXTENSIONS_DIR: /tmp/torch_extensions
          PYTORCH_NPU_ALLOC_CONF: "expandable_segments:True"
          STREAMS_PER_DEVICE: 32
          TRANSFORMERS_VERBOSITY: "error"
          GDN_ATTN_BACKEND_TRITON: 1
        run: |
          pip install sglang_router
          hf download lmms-lab/MMMU --repo-type dataset
          pip install sentence_transformers torchaudio==2.10.0
          pip install zss pre-commit wandb>=0.16.0 tenacity==8.3.0 loguru openpyxl latex2sympy2 zstandard transformers-stream-generator tqdm-multiprocess pycocoevalcap
          pip install yt-dlp sentencepiece==0.1.99 nltk av ftfy sqlitedict==2.1.0 sacrebleu>=1.5.0 pytablewriter black==24.1.0 isort==5.13.2 peft>=0.2.0 accelerate>=0.29.1
          pip install jsonlines httpx==0.25.0 evaluate>=0.4.0 datasets==2.16.1 numexpr xgrammar==0.2.1 numpy==1.26.4 dotenv
          pip install opentelemetry-sdk opentelemetry-api opentelemetry-exporter-otlp opentelemetry-exporter-otlp-proto-grpc
          pip install websocket-client websockets uvicorn==0.49.0
          git clone --branch v0.3.3 --depth 1 https://github.com/EvolvingLMMs-Lab/lmms-eval.git
          pip install opentelemetry-sdk opentelemetry-api opentelemetry-exporter-otlp opentelemetry-exporter-otlp-proto-grpc
          pip install /root/.cache/.cache/npu-pkgs/torch_memory_saver-0.0.8-cp312-cp312-linux_aarch64.whl
          cd ./lmms-eval
          nohup pip install . > lmmslog.txt 2>&1 &
          sleep 120
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          cd ../
          cd test
          python3 run_suite.py --hw npu --suite full-2-npu-a3 --nightly --continue-on-error --timeout-per-file 3600 --auto-partition-size 2 --auto-partition-id ${{ matrix.part }}

  full-4-npu-a3:
    needs: [ set-image-config ]
    runs-on: linux-aarch64-a3-4-
    strategy:
      fail-fast: false
      matrix:
        part: [0, 1]
    if: ${{ needs.set-image-config.outputs.test_scope == 'all' || needs.set-image-config.outputs.test_scope == 'func' || needs.set-image-config.outputs.test_scope == 'no_nightly' }}
    container:
      image: ${{ needs.set-image-config.outputs.image_a3 }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.set-image-config.outputs.ref || github.ref }}
      - name: Install dependencies
        env:
          TORCH_CACHE_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/whl/cpu"
          PYPI_CACHE_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple"
          UV_INDEX_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple"
          GITHUB_PROXY_URL: "https://gh-proxy.test.osinfra.cn/"
        run: |
          CACHING_URL="cache-service.nginx-pypi-cache.svc.cluster.local"
          sed -Ei "s@(ports|archive).ubuntu.com@${CACHING_URL}:8081@g" /etc/apt/sources.list
          pip config set global.index-url http://${CACHING_URL}/pypi/simple
          pip config set global.trusted-host "${CACHING_URL}"
          sglang_source_path=$(pwd)
          if [ ${{ needs.set-image-config.outputs.skip_install_flag }} != "true" ];then
            bash scripts/ci/npu/npu_ci_install_dependency.sh a3
          else
            sglang_pkg_path=/sgl-workspace/sglang/python
            ascend_test_util_path=${sglang_pkg_path}/sglang/test/ascend
            mkdir -p ${ascend_test_util_path}
            mv ${ascend_test_util_path} ${ascend_test_util_path}_bak
            cp -r ${sglang_source_path}/python/sglang/test/ascend ${ascend_test_util_path}
          fi
          cp ~/.cache/modelscope/hub/datasets/otavia/ShareGPT_Vicuna_unfiltered/ShareGPT_V3_unfiltered_cleaned_split.json /tmp
          cp ~/.cache/modelscope/hub/datasets/tmp/test.jsonl /tmp
      - name: Print Log Information
        run: bash scripts/ci/npu/npu_log_print.sh
      - name: Run test
        timeout-minutes: 960
        env:
          SGLANG_USE_MODELSCOPE: true
          SGLANG_IS_IN_CI: true
          HF_ENDPOINT: https://hf-mirror.com
          TORCH_EXTENSIONS_DIR: /tmp/torch_extensions
          PYTORCH_NPU_ALLOC_CONF: "expandable_segments:True"
          STREAMS_PER_DEVICE: 32
          TRANSFORMERS_VERBOSITY: "error"
          GDN_ATTN_BACKEND_TRITON: 1
        run: |
          pip install sglang_router
          hf download lmms-lab/MMMU --repo-type dataset
          pip install sentence_transformers torchaudio==2.10.0
          pip install zss pre-commit wandb>=0.16.0 tenacity==8.3.0 loguru openpyxl latex2sympy2 zstandard transformers-stream-generator tqdm-multiprocess pycocoevalcap
          pip install yt-dlp sentencepiece==0.1.99 nltk av ftfy sqlitedict==2.1.0 sacrebleu>=1.5.0 pytablewriter black==24.1.0 isort==5.13.2 peft>=0.2.0 accelerate>=0.29.1
          pip install jsonlines httpx==0.25.0 evaluate>=0.4.0 datasets==2.16.1 numexpr xgrammar==0.2.1 numpy==1.26.4 dotenv
          pip install /root/.cache/.cache/npu-pkgs/torch_memory_saver-0.0.8-cp312-cp312-linux_aarch64.whl
          git clone --branch v0.3.3 --depth 1 https://github.com/EvolvingLMMs-Lab/lmms-eval.git
          cd ./lmms-eval
          nohup pip install . > lmmslog.txt 2>&1 &
          sleep 120
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          cd ../
          cd test
          python3 run_suite.py --hw npu --suite full-4-npu-a3 --nightly --continue-on-error --timeout-per-file 3600 --auto-partition-size 2 --auto-partition-id ${{ matrix.part }}

  full-8-npu-a3:
    needs: [ set-image-config ]
    runs-on: linux-aarch64-a3-8-
    strategy:
      fail-fast: false
      matrix:
        part: [0, 1]
    if: ${{ needs.set-image-config.outputs.test_scope == 'all' || needs.set-image-config.outputs.test_scope == 'func' || needs.set-image-config.outputs.test_scope == 'no_nightly' }}
    container:
      image: ${{ needs.set-image-config.outputs.image_a3 }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.set-image-config.outputs.ref || github.ref }}
      - name: Install dependencies
        env:
          TORCH_CACHE_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/whl/cpu"
          PYPI_CACHE_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple"
          UV_INDEX_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple"
          GITHUB_PROXY_URL: "https://gh-proxy.test.osinfra.cn/"
        run: |
          CACHING_URL="cache-service.nginx-pypi-cache.svc.cluster.local"
          sed -Ei "s@(ports|archive).ubuntu.com@${CACHING_URL}:8081@g" /etc/apt/sources.list
          pip config set global.index-url http://${CACHING_URL}/pypi/simple
          pip config set global.trusted-host "${CACHING_URL}"
          sglang_source_path=$(pwd)
          if [ ${{ needs.set-image-config.outputs.skip_install_flag }} != "true" ];then
            bash scripts/ci/npu/npu_ci_install_dependency.sh a3
          else
            sglang_pkg_path=/sgl-workspace/sglang/python
            ascend_test_util_path=${sglang_pkg_path}/sglang/test/ascend
            mkdir -p ${ascend_test_util_path}
            mv ${ascend_test_util_path} ${ascend_test_util_path}_bak
            cp -r ${sglang_source_path}/python/sglang/test/ascend ${ascend_test_util_path}
          fi
          cp ~/.cache/modelscope/hub/datasets/otavia/ShareGPT_Vicuna_unfiltered/ShareGPT_V3_unfiltered_cleaned_split.json /tmp
          cp ~/.cache/modelscope/hub/datasets/tmp/test.jsonl /tmp
      - name: Print Log Information
        run: bash scripts/ci/npu/npu_log_print.sh
      - name: Run test
        timeout-minutes: 960
        env:
          SGLANG_USE_MODELSCOPE: true
          SGLANG_IS_IN_CI: true
          HF_ENDPOINT: https://hf-mirror.com
          TORCH_EXTENSIONS_DIR: /tmp/torch_extensions
          PYTORCH_NPU_ALLOC_CONF: "expandable_segments:True"
          STREAMS_PER_DEVICE: 32
          TRANSFORMERS_VERBOSITY: "error"
          GDN_ATTN_BACKEND_TRITON: 1
        run: |
          pip install sglang_router
          hf download lmms-lab/MMMU --repo-type dataset
          pip install sentence_transformers torchaudio==2.10.0
          pip install zss pre-commit wandb>=0.16.0 tenacity==8.3.0 loguru openpyxl latex2sympy2 zstandard transformers-stream-generator tqdm-multiprocess pycocoevalcap
          pip install yt-dlp sentencepiece==0.1.99 nltk av ftfy sqlitedict==2.1.0 sacrebleu>=1.5.0 pytablewriter black==24.1.0 isort==5.13.2 peft>=0.2.0 accelerate>=0.29.1
          pip install jsonlines httpx==0.25.0 evaluate>=0.4.0 datasets==2.16.1 numexpr xgrammar==0.2.1 numpy==1.26.4 dotenv
          git clone --branch v0.3.3 --depth 1 https://github.com/EvolvingLMMs-Lab/lmms-eval.git
          cd ./lmms-eval
          nohup pip install . > lmmslog.txt 2>&1 &
          sleep 120
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          cd ../
          cd test
          python3 run_suite.py --hw npu --suite full-8-npu-a3 --nightly --continue-on-error --timeout-per-file 3600 --auto-partition-size 2 --auto-partition-id ${{ matrix.part }}

  full-16-npu-a3:
    needs: [ set-image-config ]
    runs-on: linux-aarch64-a3-16-
    strategy:
      fail-fast: false
      matrix:
        part: [0, 1]
    if: ${{ needs.set-image-config.outputs.test_scope == 'all' || needs.set-image-config.outputs.test_scope == 'func' || needs.set-image-config.outputs.test_scope == 'no_nightly' }}
    container:
      image: ${{ needs.set-image-config.outputs.image_a3 }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.set-image-config.outputs.ref || github.ref }}
      - name: Install dependencies
        env:
          TORCH_CACHE_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/whl/cpu"
          PYPI_CACHE_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple"
          UV_INDEX_URL: "http://cache-service.nginx-pypi-cache.svc.cluster.local/pypi/simple"
          GITHUB_PROXY_URL: "https://gh-proxy.test.osinfra.cn/"
        run: |
          CACHING_URL="cache-service.nginx-pypi-cache.svc.cluster.local"
          sed -Ei "s@(ports|archive).ubuntu.com@${CACHING_URL}:8081@g" /etc/apt/sources.list
          pip config set global.index-url http://${CACHING_URL}/pypi/simple
          pip config set global.trusted-host "${CACHING_URL}"
          sglang_source_path=$(pwd)
          if [ ${{ needs.set-image-config.outputs.skip_install_flag }} != "true" ];then
            bash scripts/ci/npu/npu_ci_install_dependency.sh a3
          else
            sglang_pkg_path=/sgl-workspace/sglang/python
            ascend_test_util_path=${sglang_pkg_path}/sglang/test/ascend
            mkdir -p ${ascend_test_util_path}
            mv ${ascend_test_util_path} ${ascend_test_util_path}_bak
            cp -r ${sglang_source_path}/python/sglang/test/ascend ${ascend_test_util_path}
          fi
          cp ~/.cache/modelscope/hub/datasets/otavia/ShareGPT_Vicuna_unfiltered/ShareGPT_V3_unfiltered_cleaned_split.json /tmp
          cp ~/.cache/modelscope/hub/datasets/tmp/test.jsonl /tmp
      - name: Print Log Information
        run: bash scripts/ci/npu/npu_log_print.sh
      - name: Run test
        timeout-minutes: 960
        env:
          SGLANG_USE_MODELSCOPE: true
          SGLANG_IS_IN_CI: true
          HF_ENDPOINT: https://hf-mirror.com
          TORCH_EXTENSIONS_DIR: /tmp/torch_extensions
          PYTORCH_NPU_ALLOC_CONF: "expandable_segments:True"
          STREAMS_PER_DEVICE: 32
          TRANSFORMERS_VERBOSITY: "error"
          GDN_ATTN_BACKEND_TRITON: 1
        run: |
          pip install sglang_router
          hf download lmms-lab/MMMU --repo-type dataset
          pip install sentence_transformers torchaudio==2.10.0
          pip install zss pre-commit wandb>=0.16.0 tenacity==8.3.0 loguru openpyxl latex2sympy2 zstandard transformers-stream-generator tqdm-multiprocess pycocoevalcap
          pip install yt-dlp sentencepiece==0.1.99 nltk av ftfy sqlitedict==2.1.0 sacrebleu>=1.5.0 pytablewriter black==24.1.0 isort==5.13.2 peft>=0.2.0 accelerate>=0.29.1
          pip install jsonlines httpx==0.25.0 evaluate>=0.4.0 datasets==2.16.1 numexpr xgrammar==0.2.1 numpy==1.26.4 dotenv
          git clone --branch v0.3.3 --depth 1 https://github.com/EvolvingLMMs-Lab/lmms-eval.git
          cd ./lmms-eval
          nohup pip install . > lmmslog.txt 2>&1 &
          sleep 120
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          cd ../
          cd test
          python3 run_suite.py --hw npu --suite full-16-npu-a3 --nightly --continue-on-error --timeout-per-file 3600 --auto-partition-size 2 --auto-partition-id ${{ matrix.part }}

  nightly-poc-single-node-tests:
    name: single-node-poc
    needs: [ set-image-config ]
    if: ${{ !cancelled() && (needs.set-image-config.outputs.test_scope == 'all' || needs.set-image-config.outputs.test_scope == 'poc' || needs.set-image-config.outputs.test_scope == 'no_nightly' || needs.set-image-config.outputs.test_scope == 'poc_no_nightly') }}
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        test_config:
          - name: full-perf-2-npu-a3
            runner: linux-aarch64-a3-2
            test_type: 'perf'
          - name: full-perf-4-npu-a3
            runner: linux-aarch64-a3-4
            test_type: 'perf'
          - name: full-perf-8-npu-a3
            runner: linux-aarch64-a3-8
            test_type: 'perf'
          - name: full-perf-16-npu-a3
            runner: linux-aarch64-a3-16
            test_type: 'perf'
          - name: full-acc-2-npu-a3
            runner: linux-aarch64-a3-2-
            test_type: 'accuracy'
          - name: full-acc-4-npu-a3
            runner: linux-aarch64-a3-4-
            test_type: 'accuracy'
          - name: full-acc-8-npu-a3
            runner: linux-aarch64-a3-8-
            test_type: 'accuracy'
          - name: full-acc-16-npu-a3
            runner: linux-aarch64-a3-16-
            test_type: 'accuracy'
    uses: ./.github/workflows/_npu-single-node-test-stage.yml
    with:
      runner: ${{ matrix.test_config.runner }}
      test_type: ${{ matrix.test_config.test_type }}
      test_suite: ${{ matrix.test_config.name }}
      image: ${{ needs.set-image-config.outputs.image_a3 }}
      install_sglang_deps: false
      device_type_for_deps: 'a3'
      is_nightly_pipeline_job: true
      skip_pr_test_health_check: 'true'
      run_start_metadata: ${{ needs.set-image-config.outputs.run_start_metadata }}

  nightly-poc-multi-node-tests:
    name: multi-node-poc
    needs: [ set-image-config, nightly-poc-single-node-tests ]
    if: ${{ !cancelled() && (needs.set-image-config.outputs.test_scope == 'all' || needs.set-image-config.outputs.test_scope == 'poc' || needs.set-image-config.outputs.test_scope == 'no_nightly' || needs.set-image-config.outputs.test_scope == 'poc_no_nightly') }}
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        test_config:
          #- name: prefill_policy_bucket_balance
          #  test_case: test/registered/npu/basic_function/wip/test_npu_prefill_policy_bucket_balance.py
          #  test_type: 'perf'
          #  prefill_decode_deployment: 'separation'
          #  prefill_size: 3
          #  decode_size: 1
          #  router_size: 1
          #- name: hierarchical_cache
          #  test_case: test/registered/npu/basic_function/wip/test_npu_hierarchical_cache.py
          #  test_type: 'func'
          #  prefill_decode_deployment: 'separation'
          #  prefill_size: 1
          #  decode_size: 1
          #  router_size: 1
          #- name: fusion_operator
          #  test_case: test/registered/npu/basic_function/wip/test_npu_fusion_operator.py
          #  test_type: 'func'
          #  prefill_decode_deployment: 'separation'
          #  prefill_size: 1
          #  decode_size: 2
          #  router_size: 1
          #- name: bucket_adjust_interval_secs
          #  test_case: test/registered/npu/basic_function/wip/test_npu_bucket_adjust_interval_secs.py
          #  test_type: 'func'
          #  prefill_decode_deployment: 'separation'
          #  prefill_size: 3
          #  decode_size: 1
          #  router_size: 1
          #- name: bucket_adjust_interval_secs_concurrency
          #  test_case: test/registered/npu/basic_function/parallel_strategy/test_npu_bucket_adjust_interval_secs_concurrency.py
          #  test_type: 'func'
          #  prefill_decode_deployment: 'separation'
          #  prefill_size: 3
          #  decode_size: 1
          #  router_size: 1
          # ===== DeepSeek R1 W4A8 1p1d_16p PD separation =====
          - name: deepseek_r1_w4a8_1p1d_16p_in3k5_out1k5_50ms
            prefill_size: 1
            decode_size: 1
            router_size: 1
            test_case: test/registered/npu/performance/deepseek_r1/test_npu_deepseek_r1_w4a8_1p1d_16p_in3k5_out1k5_50ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
#          # # ===== DeepSeek R1 W8A8 2p1d_32p PD separation =====
#          - name: deepseek_r1_w8a8_2p1d_32p_in3k5_out1k5_16ms
#            prefill_size: 2
#            decode_size: 2
#            router_size: 1
#            test_case: test/registered/npu/performance/deepseek_r1/test_npu_deepseek_r1_w8a8_2p1d_32p_in3k5_out1k5_16ms.py
#            test_type: 'perf'
#            prefill_decode_deployment: 'separation'
#          - name: deepseek_r1_w8a8_2p1d_32p_in3k5_out1k5_50ms
#            prefill_size: 2
#            decode_size: 2
#            router_size: 1
#            test_case: test/registered/npu/performance/deepseek_r1/test_npu_deepseek_r1_w8a8_2p1d_32p_in3k5_out1k5_50ms.py
#            test_type: 'perf'
#            prefill_decode_deployment: 'separation'
##          - name: deepseek_r1_w8a8_2p1d_32p_in3k5_out1k_20ms
##            prefill_size: 2
##            decode_size: 2
##            router_size: 1
##            test_case: test/registered/npu/performance/deepseek_r1/test_npu_deepseek_r1_w8a8_2p1d_32p_in3k5_out1k_20ms.py
##            test_type: 'perf'
##            prefill_decode_deployment: 'separation'
##          - name: deepseek_r1_w8a8_2p1d_32p_in3k9_out1k_20ms
##            prefill_size: 2
##            decode_size: 2
##            router_size: 1
##            test_case: test/registered/npu/performance/deepseek_r1/test_npu_deepseek_r1_w8a8_2p1d_32p_in3k9_out1k_20ms.py
##            test_type: 'perf'
##            prefill_decode_deployment: 'separation'
##          - name: deepseek_r1_w8a8_2p1d_32p_in6k_out1k6_15ms
##            prefill_size: 2
##            decode_size: 2
##            router_size: 1
##            test_case: test/registered/npu/performance/deepseek_r1/test_npu_deepseek_r1_w8a8_2p1d_32p_in6k_out1k6_15ms.py
##            test_type: 'perf'
##            prefill_decode_deployment: 'separation'
##          # ===== DeepSeek V3.2 W8A8 1p1d_32p PD separation =====
##          - name: deepseek_v3_2_w8a8_1p1d_32p_in128k_out1k_26ms
##            prefill_size: 2
##            decode_size: 2
##            router_size: 1
##            test_case: test/registered/npu/performance/deepseek_v3_2/test_npu_deepseek_v3_2_w8a8_1p1d_32p_in128k_out1k_26ms.py
##            test_type: 'perf'
##            prefill_decode_deployment: 'separation'
##          - name: deepseek_v3_2_w8a8_1p1d_32p_in128k_out1k_bs16
##            prefill_size: 2
##            decode_size: 2
##            router_size: 1
##            test_case: test/registered/npu/performance/deepseek_v3_2/test_npu_deepseek_v3_2_w8a8_1p1d_32p_in128k_out1k_bs16.py
##            test_type: 'perf'
##            prefill_decode_deployment: 'separation'
##          - name: deepseek_v3_2_w8a8_1p1d_32p_in128k_out1k_bs8
##            prefill_size: 2
##            decode_size: 2
##            router_size: 1
##            test_case: test/registered/npu/performance/deepseek_v3_2/test_npu_deepseek_v3_2_w8a8_1p1d_32p_in128k_out1k_bs8.py
##            test_type: 'perf'
##            prefill_decode_deployment: 'separation'
          - name: glm5_1_w4a8_1p1d_32p_in128k_out1k_20ms
            prefill_size: 2
            decode_size: 2
            router_size: 1
            test_case: test/registered/npu/performance/glm5_1/test_npu_glm5_1_w4a8_1p1d_32p_in128k_out1k_20ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: glm5_1_w4a8_1p1d_32p_in16k_out1k_50ms
            prefill_size: 2
            decode_size: 2
            router_size: 1
            test_case: test/registered/npu/performance/glm5_1/test_npu_glm5_1_w4a8_1p1d_32p_in16k_out1k_50ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: glm5_1_w4a8_1p1d_32p_in65k_out1k5_prefix90_25ms
            prefill_size: 2
            decode_size: 2
            router_size: 1
            test_case: test/registered/npu/performance/glm5_1/test_npu_glm5_1_w4a8_1p1d_32p_in65k_out1k5_prefix90_25ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: glm5_1_w4a8_2p1d_48p_in128k_out1k_prefix90_50ms
            prefill_size: 4
            decode_size: 2
            router_size: 1
            test_case: test/registered/npu/performance/glm5_1/test_npu_glm5_1_w4a8_2p1d_48p_in128k_out1k_prefix90_50ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: glm5_1_w4a8_4p1d_48p_in64k_out1k_prefix90_50ms
            prefill_size: 4
            decode_size: 2
            router_size: 1
            test_case: test/registered/npu/performance/glm5_1/test_npu_glm5_1_w4a8_4p1d_48p_in64k_out1k_prefix90_50ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: glm5_1_w4a8_1p1d_48p_in65k_out1k5_prefix100_33ms
            prefill_size: 2
            decode_size: 4
            router_size: 1
            test_case: test/registered/npu/performance/glm5_1/test_npu_glm5_1_w4a8_1p1d_48p_in65k_out1k5_prefix100_33ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: glm5_1_w4a8_1p1d_32p_in64k_out1k_20ms
            prefill_size: 2
            decode_size: 2
            router_size: 1
            test_case: test/registered/npu/performance/glm5_1/test_npu_glm5_1_w4a8_1p1d_32p_in64k_out1k_20ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: kimi_k2_6_w4a8_1p1d_16p_in128k_out1k_100ms
            prefill_size: 1
            decode_size: 1
            router_size: 1
            test_case: test/registered/npu/performance/kimi_k2_6/test_npu_kimi_k2_6_w4a8_1p1d_16p_in128k_out1k_100ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: kimi_k2_6_w4a8_1p1d_16p_in64k_out1k5_100ms
            prefill_size: 1
            decode_size: 1
            router_size: 1
            test_case: test/registered/npu/performance/kimi_k2_6/test_npu_kimi_k2_6_w4a8_1p1d_16p_in64k_out1k5_100ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: kimi_k2_6_w4a8_1p1d_16p_in128k_out1k_prefix90_100ms
            prefill_size: 1
            decode_size: 1
            router_size: 1
            test_case: test/registered/npu/performance/kimi_k2_6/test_npu_kimi_k2_6_w4a8_1p1d_16p_in128k_out1k_prefix90_100ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: kimi_k2_6_w4a8_1p1d_16p_in64k_out1k5_prefix90_100ms
            prefill_size: 1
            decode_size: 1
            router_size: 1
            test_case: test/registered/npu/performance/kimi_k2_6/test_npu_kimi_k2_6_w4a8_1p1d_16p_in64k_out1k5_prefix90_100ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: test_npu_cp_vs_nocp_ttft
            prefill_size: 2
            decode_size: 2
            router_size: 1
            test_case: test/registered/npu/basic_function/cp/test_npu_cp_vs_nocp_ttft.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: test_npu_mimo_v2_flash_1p1d_12p_in32k_out1_ttft_5s
            prefill_size: 1
            decode_size: 1
            router_size: 1
            test_case: test/registered/npu/performance/mimo_v2_flash/test_npu_mimo_v2_flash_1p1d_12p_in32k_out1_ttft_5s.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          - name: test_npu_mimo_v2_flash_1p1d_12p_in32k_out1k_tpot_20ms
            prefill_size: 1
            decode_size: 1
            router_size: 1
            test_case: test/registered/npu/performance/mimo_v2_flash/test_npu_mimo_v2_flash_1p1d_12p_in32k_out1k_tpot_20ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          # deepseek_v4_flash performance tests
          - name: deepseek_v4_flash_w8a8_1p1d_16p_in8k_out1k_50ms
            prefill_size: 1
            decode_size: 1
            router_size: 1
            test_case: test/registered/npu/performance/deepseek_v4_flash/test_npu_deepseek_v4_flash_w8a8_1p1d_16p_in8k_out1k_50ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          # glm5.2 performance tests
          - name: glm_5_2_w4a8_4p1d_48p_in64k_out1k_prefix90_50ms
            prefill_size: 4
            decode_size: 2
            router_size: 1
            test_case: test/registered/npu/performance/glm5_2/test_npu_glm_5_2_w4a8_4p1d_48p_in64k_out1k_prefix90_50ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          # glm5.2 performance tests
          - name: glm_5_2_w4a8_2p1d_32p_in64k_out1k_prefix90_20ms
            prefill_size: 2
            decode_size: 2
            router_size: 1
            test_case: test/registered/npu/performance/glm5_2/test_npu_glm_5_2_w4a8_2p1d_32p_in64k_out1k_prefix90_20ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          # glm5.2 performance tests
          - name: glm_5_2_w4a8_2p1d_32p_in128k_out1k_prefix90_20ms
            prefill_size: 2
            decode_size: 2
            router_size: 1
            test_case: test/registered/npu/performance/glm5_2/test_npu_glm_5_2_w4a8_2p1d_32p_in128k_out1k_prefix90_20ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          # glm5.2 performance tests
          - name: glm_5_2_w4a8_3p1d_32p_in16k_out1k_50ms
            prefill_size: 3
            decode_size: 1
            router_size: 1
            test_case: test/registered/npu/performance/glm5_2/test_npu_glm_5_2_w4a8_3p1d_32p_in16k_out1k_50ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
          # glm5.2 performance tests
          - name: glm_5_2_w4a8_4p1d_48p_in128k_out1k_prefix90_50ms
            prefill_size: 4
            decode_size: 2
            router_size: 1
            test_case: test/registered/npu/performance/glm5_2/test_npu_glm_5_2_w4a8_4p1d_48p_in128k_out1k_prefix90_50ms.py
            test_type: 'perf'
            prefill_decode_deployment: 'separation'
    uses: ./.github/workflows/nightly-test-npu-e2e-multi-node.yml
    with:
      runner: linux-amd64-cpu-8
      test_type: ${{ matrix.test_config.test_type }}
      test_config_name: ${{ matrix.test_config.name }}
      prefill_size: ${{ matrix.test_config.prefill_size }}
      decode_size: ${{ matrix.test_config.decode_size }}
      router_size: ${{ matrix.test_config.router_size }}
      test_case: ${{ matrix.test_config.test_case }}
      image: ${{ needs.set-image-config.outputs.image_a3 }}
      install_sglang_from_source: false
      prefill_decode_deployment: ${{ matrix.test_config.prefill_decode_deployment }}
      transformers_version: ''
      run_start_metadata: ${{ needs.set-image-config.outputs.run_start_metadata }}

  nightly-poc-multi-node-mix-tests:
    name: multi-node-mix-poc
    needs: [ set-image-config, nightly-poc-single-node-tests, nightly-poc-multi-node-tests ]
    if: ${{ !cancelled() && (needs.set-image-config.outputs.test_scope == 'all' || needs.set-image-config.outputs.test_scope == 'poc' || needs.set-image-config.outputs.test_scope == 'no_nightly' || needs.set-image-config.outputs.test_scope == 'poc_no_nightly') }}
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        test_config:
          - name: glm5_1_w4a8_16p_in3k5_out1k5_50ms
            node_size: 2
            test_case: test/registered/npu/performance/glm5_1/test_npu_glm5_1_w4a8_16p_in3k5_out1k5_50ms.py
            test_type: 'perf'
    uses: ./.github/workflows/nightly-test-npu-e2e-multi-node.yml
    with:
      runner: linux-amd64-cpu-8
      test_type: 'perf'
      test_config_name: ${{ matrix.test_config.name }}
      node_size: ${{ matrix.test_config.node_size }}
      test_case: ${{ matrix.test_config.test_case }}
      image: ${{ needs.set-image-config.outputs.image_a3 }}
      install_sglang_from_source: false
      prefill_decode_deployment: 'mix'
      transformers_version: ''
      run_start_metadata: ${{ needs.set-image-config.outputs.run_start_metadata }}

  check-all-jobs:
    needs:
      - set-image-config
      - nighly-test-npu
      - full-1-npu-a3
      - full-2-npu-a3
      - full-4-npu-a3
      - full-8-npu-a3
      - full-16-npu-a3
      - nightly-poc-single-node-tests
      - nightly-poc-multi-node-tests
      - nightly-poc-multi-node-mix-tests
    runs-on: ubuntu-latest
    if: ${{ !cancelled() }}
    steps:
      - name: Download all metrics
        uses: actions/download-artifact@v4
        with:
          pattern: metrics-*
          path: /tmp/metrics
          merge-multiple: false

      - name: Generate results table
        run: |
          echo "## Per-Test Metrics" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          has_metrics=false
          for dir in /tmp/metrics/metrics-*/; do
            file="${dir}metrics.json"
            if [ -f "$file" ]; then
              has_metrics=true
              echo "import json" > /tmp/parse_metrics.py
              echo "with open('$file') as f:" >> /tmp/parse_metrics.py
              echo "    d = json.load(f)" >> /tmp/parse_metrics.py
              echo "tc = d.get('test_case', '-')" >> /tmp/parse_metrics.py
              echo "tp = d.get('test_type', '-')" >> /tmp/parse_metrics.py
              echo "st = d.get('status', 'unknown')" >> /tmp/parse_metrics.py
              echo "icon = '✅' if st == 'pass' else '❌'" >> /tmp/parse_metrics.py
              echo "metrics = d.get('metrics', {})" >> /tmp/parse_metrics.py
              echo "baselines = d.get('baselines', {})" >> /tmp/parse_metrics.py
              echo "mstr = ', '.join(f'{k}={v}' for k,v in metrics.items()) if metrics else '-'" >> /tmp/parse_metrics.py
              echo "bstr = ', '.join(f'{k}={v}' for k,v in baselines.items()) if baselines else '-'" >> /tmp/parse_metrics.py
              echo "print(f'| {tc} | {tp} | {icon} {st} | {mstr} | {bstr} |')" >> /tmp/parse_metrics.py
              python3 /tmp/parse_metrics.py
            fi
          done > /tmp/metrics_table.txt

          if [ "${has_metrics}" = "true" ]; then
            echo "| Test Case | Type | Status | Metrics | Baseline |" >> $GITHUB_STEP_SUMMARY
            echo "|-----------|------|--------|---------|----------|" >> $GITHUB_STEP_SUMMARY
            cat /tmp/metrics_table.txt >> $GITHUB_STEP_SUMMARY
          else
            echo "No per-test metrics available (artifacts not found)." >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY

          FAIL=0
          ${{ needs.nighly-test-npu.result != 'success' && needs.nighly-test-npu.result != 'skipped' }} && FAIL=1
          exit $FAIL
